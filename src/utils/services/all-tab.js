// i18n key for the synthetic, admin-only "All services & bookmarks" tab (M10a).
// It isn't backed by settings.yaml — the translated label itself doubles as
// the tab's identity (compared via slugifyAndEncode, like any real tab), so
// there's no separate id to keep in sync with a config value.
export const ALL_TAB_LABEL_KEY = "dashboard.allServicesTab";
